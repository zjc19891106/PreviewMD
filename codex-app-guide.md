# CodeX App 详细使用文档（桌面版）

> 面向开发者的实践型使用手册，覆盖从启动、项目接入到高质量协作与自动化的完整流程。

## 适用人群

- 希望用 AI 辅助完成编码、审查、重构、文档与排障的开发者
- 需要在本地项目中高效执行“读-改-测-验”闭环的人
- 期望以可控、可审计的方式使用自动化与工具链的人

---

## 1. 核心概念

**会话（Session）**：一次任务沟通的上下文边界。建议“一事一会话”。

**工作区（Workspace）**：你打开的项目目录，工具可以在此范围内读写与执行命令。

**上下文（Context）**：模型当前可见的信息集合（你提供的描述 + 文件 + 终端输出 + 其它资料）。

**工具（Tools）**：包含读写文件、执行命令、获取网页信息、生成图片等能力。工具通常需要确认或被允许后才能运行。

---

## 2. 快速开始（最短路径）

1. 打开 CodeX App 并选择/打开项目目录（工作区）。
2. 在会话里说明目标、范围与约束：
   - 目标：要实现什么
   - 范围：涉及哪些目录/文件
   - 约束：技术栈、风格、禁止事项
   - 验收：你期望的结果/测试
3. 让 App 先“读”再“改”：例如“先概览项目结构，再提出修改方案”。
4. 确认方案后执行修改，最后运行测试或给出验证建议。

---

## 3. 高质量提问模板（强烈建议）

**建议模板**：

```
目标：
范围：
技术栈/约束：
验收标准：
已知问题/风险：
```

**示例**：

```
目标：为支付模块新增退款接口
范围：src/api/payments, src/services/payment
技术栈/约束：Node.js + TypeScript + Express；不引入新依赖
验收标准：通过现有单测，新增 3 个退款用例
已知问题/风险：现有支付逻辑较耦合，先给出改动方案
```

---

## 4. 项目接入与上下文管理

### 4.1 打开工作区
- 将项目目录作为工作区打开，可让工具读写文件与执行命令。
- 建议保持“最小权限原则”：只打开需要操作的目录。

### 4.2 精准控制上下文
- 不要把整个仓库一次性塞给模型。建议从入口文件、关键模块开始。
- 对超大仓库，先让 App 生成结构概览，再逐步深入。

### 4.3 处理敏感信息
- 不要直接粘贴密钥、私有证书、生产环境配置。
- 如需说明流程，用假值或脱敏值代替。

---

## 5. 常见任务工作流

### 5.1 新增功能
1. 需求澄清 → 2. 方案设计 → 3. 代码修改 → 4. 测试验证 → 5. 变更说明

提示语示例：
```
请先分析当前模块结构，给出实现方案（含影响范围和风险点），我确认后再修改代码。
```

### 5.2 修复 Bug
1. 复现路径 → 2. 可能原因 → 3. 最小改动修复 → 4. 回归测试

提示语示例：
```
这是复现步骤和日志，请先定位根因并给出修复方案，再提交修改。
```

### 5.3 代码重构
1. 识别重复/复杂模块 → 2. 给出拆分方案 → 3. 小步重构 → 4. 保持接口不变

提示语示例：
```
请找出该模块的主要复杂点与重复逻辑，给出最小重构计划（分步骤）。
```

### 5.4 文档与规范
- 让 App 先提纲，再逐步完善。
- 明确受众（新人/团队/外部用户）与深度层级。

---

## 6. 工具使用与权限意识

### 6.1 工具类型
- **文件工具**：读取、编辑、创建文件
- **命令工具**：执行 shell 命令（例如测试、构建、格式化）
- **网络工具**：检索公开资料（当你明确要求时）
- **图像工具**：生成或解释图片

### 6.2 权限与确认
- 对潜在风险操作（删除、批量修改、发布等）建议主动要求确认。
- 你可以指定：
  - “先输出变更计划，再动手”
  - “只改指定文件”
  - “每一步都先确认”

---

## 7. 让输出可审计与可维护

### 7.1 强制变更说明
建议要求：
- 变更点列表
- 影响范围
- 风险与回滚建议
- 测试建议

### 7.2 差异审查
- 让 App 逐文件说明修改理由。
- 对关键模块，要求提供“替代方案 + 取舍理由”。

---

## 8. 多模态能力（可选）

- **上传截图**：用于分析 UI、错误提示、日志截图。
- **上传示意图**：用于生成对应的代码结构或布局草案。
- **生成图片**：可用于概念图、图标草图或文档配图。

---

## 9. 自动化与重复任务（如果你需要）

适合场景：
- 周期性生成报告
- 定时检查依赖更新
- 定期运行质量扫描

建议做法：
- 把自动化任务写成清晰的“输入/输出/触发条件”。
- 输出路径与格式要固定，方便持续使用。

---

## 10. 常见问题与实践建议

**Q1：回答不够稳定怎么办？**
- 限定范围和约束，减少自由度。
- 分阶段执行：先分析，再计划，最后修改。

**Q2：怎么降低出错概率？**
- 指定“最小改动”原则。
- 让 App 给出备选方案与风险点。

**Q3：如何避免不必要的改动？**
- 明确“禁止修改”的文件/目录。
- 对每次变更要求简短理由。

**Q4：如何保证质量？**
- 要求执行或给出测试步骤。
- 要求列出可能破坏的边界场景。

---

## 11. 进阶提示（高效协作习惯）

- **小步快跑**：拆成小任务逐个完成、逐个验收。
- **结构优先**：先对架构与模块边界达成共识，再写实现细节。
- **显式约束**：把“不能做什么”写清楚。
- **持续反馈**：对每次结果指出“哪里好、哪里不够”。

---

## 12. 推荐的会话启动语

```
这是一个 X 项目，技术栈是 Y。
目标是 Z。
约束：不引入新依赖；不修改 A/B 目录；保证现有测试通过。
请先给出改动方案与风险点，我确认后再修改。
```

---

## 13. 版本与更新说明

本文档为通用使用指南，若 App 版本或功能更新，请以实际界面与官方公告为准。

